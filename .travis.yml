language: python

python:
  - 3.5
  - 3.6

services:
  - postgresql

install:
  - pip install -r requirements-dev.txt

before_scripts:
  - psql -c 'create database travis_ci_db;' -U postgres

script:
  - ./ci.sh

after_success:
  - codecov
